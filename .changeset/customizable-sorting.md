---
'ha-treemap-card': minor
---

Add customizable sorting to control item order independently from sizing. Items can now be sorted by value (default), entity_id, label, or config order. Perfect for battery monitoring grids and status dashboards where predictable positioning matters.
